# pnpm configuration
enable-pre-post-scripts=true
auto-install-peers=true

# Audit configuration
# Note: This audit override is for documentation purposes only
# The validator@13.15.15 vulnerability (GHSA-9965-vmph-33xx) affects URL validation
# It is only used transitively through swagger-jsdoc for API documentation generation
# Risk assessment: LOW - not used in production request handling or user input validation
# Status: No patch available as of 2025-10-22 (patched_versions: <0.0.0)
# Decision: Accepted risk - will monitor for updates
audit-level=high
